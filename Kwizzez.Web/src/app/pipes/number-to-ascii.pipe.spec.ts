import { NumberToASCIIPipe } from './number-to-ascii.pipe';

describe('NumberToASCIIPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToASCIIPipe();
    expect(pipe).toBeTruthy();
  });
});
